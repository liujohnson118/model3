<div id='map'></div>
<dl>
    <dt class="smallBattery">Aqua</dt>
    <dd>60kWh</dd>
    <dt class="mediumBattery">Black</dt>
    <dd>70 kWh</dd>
    <dt class="largeBattery">Orange</dt>
    <dd>80 kWh</dd>
</dl>
<p>As of now Tesla model3 with a 60kWh battery can travel <p id='actual_range_60'><%=Consumption.where(battery: 60).average(:range).to_s %></p> km on a full charge</p>
<p>As of now Tesla model3 with a 70kWh battery can travel <p id='actual_range_70'><%=Consumption.where(battery: 70).average(:range).to_s %></p> km on a full charge</p>
<p>As of now Tesla model3 with a 80kWh battery can travel <p id='actual_range_80'><%=Consumption.where(battery: 80).average(:range).to_s %></p> km on a full charge</p>
<div id='all_user_revies'>
  <%= render @reviews %>
</div>

<%= javascript_include_tag('actual_range.js') %>
<script async defer
src="https://maps.googleapis.com/maps/api/js?key=<%= Rails.application.secrets[:google_maps_api_key] %>&callback=actualRangeMap">
</script>